<div class="container">
  <%= form_with(model: @pokemon_acquisition) do |form| %>

    <div>
      <%= form.label :name, style: "display: block" %>
      <%= form.text_field :name %>
    </div>

    <div>
      <%= form.submit %>
    </div>
  <% end %>

</div>