<%= form_with(model: pokemon_purchase) do |form| %>
  <% if pokemon_purchase.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pokemon_purchase.errors.count, "error") %> prohibited this pokemon_purchase from being saved:</h2>

      <ul>
        <% pokemon_purchase.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :value, style: "display: block" %>
    <%= form.text_field :value %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
